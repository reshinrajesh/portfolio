import LoginPageClient from "./page.client";

export const dynamic = "force-dynamic";

export const metadata = {
    title: "Admin Login | Res.",
};

export default function LoginPage() {
    return <LoginPageClient />;
}
