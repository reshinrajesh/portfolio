import VerifyPageClient from "./page.client";

export const dynamic = "force-dynamic";

export const metadata = {
    title: "Verify Security Key | Res.",
};

export default function VerifyPage() {
    return <VerifyPageClient />;
}
