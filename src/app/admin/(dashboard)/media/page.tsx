

import MediaClient from "./MediaClient";

export const dynamic = "force-dynamic";

export const metadata = {
    title: "Media Gallery | Res.",
};

export default function Page() {
    return <MediaClient />;
}
